import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './assets/App.jsx'; // Importa o componente App que está na pasta assets

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);